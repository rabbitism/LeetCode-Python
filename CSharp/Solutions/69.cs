using System;

namespace CSharp{
    
    public partial class Solution{

        //Use Build In Function, accepted.
        public int MySqrt(int x) {
            return (int)Math.Floor(Math.Sqrt(x));
        }
    }
}